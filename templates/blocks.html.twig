
{% block head %}
{% endblock %}

{% block module_page_header %}
    <div class="header">
        <img src="{{ resources('images/icon.png', module.chave) }}" width="50"/>
        <h2>{{ module.nome }}</h2>
        <p>{{ module.descricao }}</p>
    </div>
{% endblock %}
